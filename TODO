* each beer has own scheme, but abstract:
    - glucanrest
    - ferularest
    - proteinrest
    - saccharification
    - mash out
* add option to relate one batch to another for slurry use

* make shopping list, based on inventory, for planned batches.
  - configurable for a given date.

* add warning when inventory not sufficient for planned brews

* use batch projected volume and subtract losses to calculate brew
  volumes. If the produced volume is less than the start volume minus
  all losses in transfers, warn.

* calculate timings of brewhouse based on measurements

* Make metaphase measurements into targets for brew/batch

* Tanks should go into a registry for listing types, for example when
  one needs to add a tank of a certain type.

* Batch materials should follow from deliverables and ingredients.

* style from BJCP seem very incomplete. Paging?

* When a task is defined, it should also generate tasks based on what
  is already there, like brews, etc.
