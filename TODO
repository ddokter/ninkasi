* use doc string of class for page listing
* make phase more clear: phases for measurement/phases in brewing.
* each beer has own scheme, but abstract:
    - glucanrest
    - ferularest
    - proteinrest
    - saccharification
    - mash out
* add settings for critical parameters, like warm-up time
* add option to relate one batch to another for slurry use

* make shopping list, based on inventory, for planned batches.
  - configurable for a given date.
* add warning when inventory not sufficient for planned brews

* pass around duration values as-is, so the receiver can determine how
to show